const canAccess = async (therapistId, featureKey) => {
  // Actual subscription entitlement logic
  // subscription module ke time implement hoga.

  return true;
};

module.exports = {
  canAccess,
};